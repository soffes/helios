- id: leave_home
  alias: Leave Home
  mode: single
  trigger:
    - trigger: state
      entity_id: person.sam
      to: "not_home"
  action:
    - action: scene.turn_on
      target:
        entity_id: scene.away
